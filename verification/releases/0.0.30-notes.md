Cursor drawing now paints immediately when enabled. An explicit **Draw with cursor** checkbox switches between painting and repositioning. Taps place dots; short strokes are preserved instead of being cancelled or toggling ink off. Circle/square cursor outlines, the cursor magnifier and undo remain available, and draw/move mode persists in drafts.

The main canvas, floating selection preview and magnifier display pixels without smoothing, including below 100% zoom. Drawing antialiasing defaults off.

JPEG XL lossless saving is verified through File → Save as → JPEG XL and a subsequent File → Save, checking all 49,601 RGB pixels after reopening each output. The native codec also has exact round-trip and chunk-boundary tests. Export as JPEG XL uses lossy quality settings; lossless Save as preserves the application's opaque 8-bit sRGB canvas, not arbitrary source metadata or original HDR data.

Package: `paint.anpaint.android`; version code: 83. Universal APK for Android API 21 and later, signed with the existing certificate for compatible upgrades. Matching corresponding source, checksums and verification evidence are included.

Verification: 75 local host checks; 257 release regression tests; zero app lint issues; 77 instrumentation tests on API 30 and 78 on API 35. All passed.

[Build and checks](https://github.com/c933103/AN-Paint/actions/runs/34945326732).
